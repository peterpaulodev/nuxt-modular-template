{
  "name": "nuxt-modular-template",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "lint:fix": "pnpm --filter web lint:fix",
    "test": "pnpm --filter web test",
    "prepare": "husky",
    "create:module": "node scripts/create-module.mjs",
    "create:ui": "node scripts/create-ui.mjs",
    "create:page": "node scripts/create-page.mjs",
    "create:feature": "node scripts/create-feature.mjs",
    "storybook": "pnpm --filter web storybook",
    "format": "pnpm --filter web format"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7",
    "sass": "^1.101.0"
  }
}
