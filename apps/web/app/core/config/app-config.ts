// Configurações estáticas da aplicação — não variam por ambiente.
// Para configurações que mudam por ambiente (URL de API, nome da app, feature flags),
// use useAppConfig() de @core/config/runtime-config.ts.
export const APP_CONFIG = {
  version: '1.0.0',
}
